doctype html
html
  head
    title= title || "Dorm Supplies"
    link(rel='stylesheet', href='/stylesheets/style.css')
    script(src='/js/fetch.js')
  body
    script(src='/js/script.min.js')
    block content
    if token
        script localStorage.token = "#{token}"
    block postScript
    if dev
        script(src='//localhost:10000/livereload.js')
